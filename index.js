const mineflayer = require('mineflayer');const botArgs = { host: 'gais_9009.aternos.me:49959', // Замени на IP своего сервера port: 25565, // Порт обычно стандартный username: 'AFK_Bot', // Ник бота version: '1.21.1' // Версия твоего сервера};const initBot = () => { const bot = mineflayer.createBot(botArgs); bot.on('login', () => { console.log('✅ Бот зашел на сервер!'); }); // Анти-АФК: бот будет иногда прыгать, чтобы его не кикнуло bot.on('spawn', () => { setInterval(() => { bot.setControlState('jump', true); setTimeout(() => bot.setControlState('jump', false), 500); }, 30000); // Прыгает каждые 30 секунд }); // Авто-перезаход, если сервер выключился или бот вылетел bot.on('end', () => { console.log('❌ Соединение потеряно. Переподключение через 10 секунд...'); setTimeout(initBot, 10000); }); bot.on('error', (err) => console.log('Ошибка:', err));};initBot();